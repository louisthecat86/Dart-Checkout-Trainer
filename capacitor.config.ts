import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.braadart.checkouttrainer',
  appName: 'Dart Checkout Trainer',
  webDir: 'dist'
};

export default config;
